{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Run Current Java File",
            "request": "launch",
            "mainClass": "${fileBasenameNoExtension}",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Compile Current Folder Java Files",
            "classPaths": [
                "${fileDirname}"
            ],
            "console": "integratedTerminal"
        }
    ]
}
